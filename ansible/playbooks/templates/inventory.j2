[all]
{% for host in ansible_hosts.split('\n') | select('string') | reject('match', '^\s*$') | list %}
{{ host }} ansible_user={{ ansible_user }} ansible_python_interpreter=/usr/bin/python3{% if ansible_ssh_private_key_file is defined and ansible_ssh_private_key_file | length > 0 %} ansible_ssh_private_key_file={{ ansible_ssh_private_key_file }}{% endif %}

{% endfor %}
