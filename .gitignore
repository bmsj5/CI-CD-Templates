# CI/CD and Infrastructure Automation .gitignore
# This repository contains Ansible playbooks, Helm charts, and CI/CD templates

# Operating System Files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
*~

# Temporary Files
*.tmp
*.temp
*.bak
*.swp
*.swo
*~

# Logs
*.log
logs/
log/

# Ansible-specific
# Ansible retry files
*.retry
# Ansible vault password files (if used)
.vault_pass
# Ansible temporary files
ansible*.cfg.tmp
# Ansible local directory (created by ansible-playbook --connection=local)
.ansible/

# Kubernetes/Helm
# Helm chart packages
*.tgz
# Kubernetes generated YAML files (if any)
/kustomize/
/overlays/
/patches/

# GitHub Actions
# Action temporary files
.github/actions/*/dist/
# Workflow artifacts
artifacts/
# Test results
test-results/

# Docker
# Docker build context
.dockerignore.tmp
# Docker volumes (if any local testing)
/docker-volumes/

# Environment and Secrets
# Environment files (keep templates, ignore actual values)
.env
.env.*
!.env.example
!.env.template
# Secret files (your secrets/ directory should be encrypted or in private repo)
secrets.decrypted/
*.key
*.pem
*.crt
*.p12

# IDE and Editor Files
# VS Code
.vscode/
!.vscode/settings.json.template
!.vscode/extensions.json
# JetBrains IDEs
.idea/
*.iml
*.ipr
*.iws
# Vim
*.swp
*.swo
# Emacs
*~
\#*\#
/.emacs.desktop
/.emacs.desktop.lock
*.elc
auto-save-list
tramp
.\#*
.org-id-locations
*_archive
# Sublime Text
*.tmlanguage.cache
*.tmPreferences.cache
*.stTheme.cache
*.sublime-workspace
*.sublime-project

# Build and Cache Directories
.cache/
build/
dist/
target/
out/

# Backup Files
*.orig
*.rej

# Shell History
.bash_history
.zsh_history

# Python (minimal - only if you have Python scripts)
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
pip-log.txt
pip-delete-this-directory.txt
.coverage
.pytest_cache/
.tox/
.env
.venv
env/
venv/

# Node.js (if you add any frontend tooling)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm
.yarn-integrity

# Terraform (if you add infrastructure as code)
.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl

# Ansible Galaxy roles (if downloaded locally)
roles/
collections/